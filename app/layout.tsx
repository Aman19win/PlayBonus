export const metadata = { title: "PlayBonus", description: "Claim Bonus demo" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
